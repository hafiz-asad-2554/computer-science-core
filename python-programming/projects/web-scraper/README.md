# Simple Web Scraper

Uses `requests` and `count` to simulate scraping.
Note: Since I didn't want to install `BeautifulSoup` globally in my lab exams, this script manually parses strings.

## Logic

1. Fetch HTML from a URL (using a dummy function here to simulate it since I don't have internet access).
2. Count occurrences of a keyword.
