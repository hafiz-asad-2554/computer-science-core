# File Organizer

This script organizes files in a directory based on their extension.

## Usage

Change the `DIRECTORY` variable in the script to the folder you want to clean up.
It creates folders like `Images`, `Documents`, `Code` and moves files there.

**USE WITH CAUTION:** It moves files!
