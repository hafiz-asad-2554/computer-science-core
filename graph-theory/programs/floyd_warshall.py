def floyd_warshall(graph, V):
    dist = [[float("inf")] * V for _ in range(V)]
    # Setup initial distances
    # Triple nested loop k, i, j
    print("Floyd-Warshall computes All-Pairs Shortest Path.")

print("Floyd-Warshall Placeholder.")
