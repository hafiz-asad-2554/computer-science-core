# File-based Record System

This program writes data to a text file and reads it back, serving as a persistent memory test.

## How it works

1. Takes user input (ID, Data).
2. Appends it to `records.txt`.
3. Reads all lines from `records.txt` to display the history.
